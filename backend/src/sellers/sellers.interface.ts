export namespace Sellers {
    export interface Create {
        id: string,
        username: string,
        password: string,
        user_type: string,
        created_at: string,
        updated_at: string
    }
}